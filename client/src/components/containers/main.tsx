import styled from 'styled-components';

const StyledMain = styled.main`
  flex: 1;
  overflow: auto;
`;

const Main = () => (
  <StyledMain>
    <div>testing</div>
  </StyledMain>
);

export default Main;
